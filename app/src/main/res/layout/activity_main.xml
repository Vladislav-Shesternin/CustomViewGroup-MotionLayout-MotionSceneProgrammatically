<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <com.veldan.customviewgroup_with_children_customview.CustomViewGroup
        android:id="@+id/customViewGroup"
        android:layout_width="70dp"
        android:layout_height="70dp"
        android:background="@color/purple_200"
        app:direction="RIGHT"
        app:image="@drawable/ic_home_24"
        app:layoutDescription="@xml/place_holder_scene"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <com.veldan.customviewgroup_with_children_customview.CustomView
            android:id="@+id/customView1"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="@color/purple_500"
            android:src="@drawable/ic_image_24" />

        <com.veldan.customviewgroup_with_children_customview.CustomView
            android:id="@+id/customView2"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="@color/purple_500"
            android:src="@drawable/ic_send_24" />

    </com.veldan.customviewgroup_with_children_customview.CustomViewGroup>

</androidx.constraintlayout.widget.ConstraintLayout>